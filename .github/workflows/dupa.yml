name: Build

on:
  push:
    branches:
      - '**'



jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: dupa
        uses: pawelbanasiak/github-actions/.github/actions/compress@master
        with:
          dir: dupa-dir
      - name: test
        run: echo "${GITHUB_REF_NAME}"


